<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# Гра Престолів: Тіні Вестеросу

Інтерактивна текстова гра у всесвіті "Гра Престолів". Тепер логіка штучного інтелекту працює через окремий бекенд, тому API ключ більше не потрапляє у фронтенд-бандл.

## Структура

```
Game-of-Thrones/
├── src / components / services ... (фронтенд Vite + React)
├── server/ (Express + TypeScript, REST API до Gemini)
├── shared/ (спільні типи та константи)
└── docs/ (статичний білд для GitHub Pages, якщо потрібно)
```

## Локальний запуск

**Вимоги:** Node.js 18+ (та npm).

1. **Встановіть залежності:**
   ```bash
   npm install
   cd server && npm install
   ```

2. **Налаштуйте змінні середовища:**
   ```bash
   cp .env.example .env
   cp server/.env.example server/.env
   ```
   - У `server/.env` пропишіть свій `GEMINI_API_KEY` (цей файл не комітимо).
   - У `.env` (фронт) за замовчуванням стоїть `VITE_API_BASE_URL=http://localhost:4000`. Після деплою бекенда замініть на бойовий URL.

3. **Запустіть бекенд:**
   ```bash
   cd server
   npm run dev
   ```
   Сервер слухає порт `4000` та надає ендпоінти `/api/story/*` і `/api/images/*`.

4. **Запустіть фронтенд у іншому терміналі:**
   ```bash
   npm run dev
   ```
   Відкрийте `http://localhost:3000`.

## REST API (backend)

| Метод | Шлях | Опис |
|-------|------|------|
| POST | `/api/story/start` | Стартова сцена (body: `{character}`) |
| POST | `/api/story/turn` | Наступний хід (body: `{character, history, lastChoice, turnCount, maxTurns}`) |
| POST | `/api/images/scene` | Кадр сцени (body: `{visualDescription}`) |
| POST | `/api/images/portrait` | Портрет NPC/героя (body: `{name}`) |

Формат відповіді для сюжетних ендпоінтів — `StoryNode`, ідентичний тому, що використовує фронт.

## Збірка

### Фронтенд
```bash
npm run build
```
Файли потраплять у `dist/`.

### Бекенд
```bash
cd server
npm run build   # збирає у server/dist
npm start       # запускає зібраний код
```

## Деплой на Vercel

### 1. Бекенд (окремий проєкт)
1. У Vercel створіть проєкт і як корінь вкажіть папку `server`.
2. Build command: `npm run build`, Output dir: `dist` (Vercel потім викличе `npm start`).
3. В Environment Variables додайте `GEMINI_API_KEY`, за потреби `ALLOWED_ORIGINS`, `GEMINI_TEXT_MODEL`, `GEMINI_IMAGE_MODEL`.
4. Після деплою отримаєте URL на кшталт `https://got-ai-server.vercel.app`.

### 2. Фронтенд
1. У `.env` впишіть `VITE_API_BASE_URL=https://got-ai-server.vercel.app` (або власний домен бекенда).
2. Зберіть проєкт (`npm run build`).
3. Задеплойте `dist/` будь-де: Vercel (Static Site), GitHub Pages, Cloudflare Pages тощо. Головне — щоб клієнт міг звертатися до бекенда.

> Для GitHub Pages секрет більше не потрібен. У workflow можна просто збирати фронт, а бекенд жити окремо на Vercel/Render.

## Безпека ключа
- API ключ знаходиться лише у `server/.env` і не вбудовується у браузерний код.
- Навіть якщо хтось завантажить `docs/` або `dist/`, там немає секретів.
- Якщо старий ключ було скомпрометовано, згенеруйте новий у Google AI Studio і замініть лише на бекенді.

## Корисні команди
| Команда | Опис |
|---------|------|
| `npm run dev` | Dev-сервер фронта (Vite) |
| `npm run build` | Збірка фронта |
| `npm run preview` | Перевірка зібраного фронта |
| `cd server && npm run dev` | Dev-режим Express (tsx watch) |
| `cd server && npm run build` | Збірка бекенда |
| `cd server && npm start` | Запуск зібраного бекенда |

## Технології
- React 19 + TypeScript + Vite
- Express + TypeScript (бекенд)
- Google Gemini (REST API)
- Vercel / GitHub Pages для деплою

Якщо потрібні додаткові інструкції або автоматизація — відкрийте issue ���
